dependencies:
  override:
    - gem install mdl
    # Disable sudo once CircleCI is not bugged anymore
    - sudo pip install flake8

test:
  override:
    - mdl:
        files:
          - "**/*.md"
    - flake8:
        files:
          - "**/*.py"
